# 部署敏感配置（复制为 .env.deploy 并填入实际值，.env.deploy 已 gitignore）
# SSH 登录地址（如 user@example.com）
DEPLOY_SSH_HOST=
# 服务器上代码目录（rsync 目标）
DEPLOY_REMOTE_CODE_DIR=
# 服务器上 docker compose 编排目录
DEPLOY_REMOTE_COMPOSE_DIR=
# 健康检查 URL
DEPLOY_HEALTH_URL=
# 用于校验静态资源 hash 的页面 URL
DEPLOY_TOOLS_URL=
